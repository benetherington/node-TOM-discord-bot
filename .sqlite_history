tables
.open .data/title-suggestions.db
.tables
CREATE TABLE Episodes (
episode_id INTEGER PRIMARY KEY,
ep_num     INTEGER,
created_at TEXT NOT NULL DEFAULT (DATETIME('now', 'localtime')),
updated_at TEXT NOT NULL DEFAULT (DATETIME('now', 'localtime))
)
.tables
CRWATE TEMPORARY TABLE temp_table1 (name TEXT);
CREATE TABLE Episodes (
episode_id INTEGER PRIMARY KEY,
ep_num     INTEGER,
created_at TEXT NOT NULL DEFAULT (DATETIME('now', 'localtime')),
updated_at TEXT NOT NULL DEFAULT (DATETIME('now', 'localtime))
);
);
CREATE TABLE episodes (episode_id INTEGER PRIMARY KEY, ep_num INTEGER);
.tables
drop table episodes;
CREATE TABLE Episodes (episode_id INTEGER PRIMARY KEY, ep_num INTEGER, created_at TEXT NOT NULL DEFAULT(DATETIME('now', 'localtime')), updated_at TEXT NOT NULL DEFAULT(DATETIME('now', 'localtime')));
.tables
INSERT INTO Episodes (ep_num) VALUES (123)
;
.help
.dump Episodes
SELECT * FROM Episodes
;
.headers on
SELECT * FROM Episodes;
INSERT INTO Episodes (ep_num) VALUES (124);
SELECT * FROM Episodes;
.mode column Episodes
SELECT * FROM Episodes;
.mode column *
SELECT * FROM Suggestions
;
SELECT episode_id FROM Episodes WHERE ep_num=123
;
INSERT INTO Suggestions (episode_id, author_id, message_id, jump_url, suggestion) VALUES (1, 456, 654, discord.com/jump, title of tests)
;
INSERT INTO Suggestions (episode_id, author_id, message_id, jump_url, suggestion) VALUES (1, 456, 654, 'discord.com/jump', 'title of tests');
SELECT * FROM Suggestions;
INSERT INTO Suggestions (episode_id, author_id, message_id, jump_url, suggestion) VALUES (1, 456, 655, 'discord.com/jump', "test of titles");
SELECT * FROM Suggestions;
.quit
.tables
.dump migrations
INSERT INTO Authors (discord_id, name) VALUES (1234, "Ben)
;
INSERT INTO Authors (discord_id, name) VALUES (1234, "Ben);
;
)
)help
.help
.tables
.dump Authors
.help
.schema Authors
SELECT * FROM Authors;
INSERT INTO Authors (discord_id, name, nick) VALUES (123, "Benjamin", "Ben")
;
INSERT INTO Authors (discord_id, name, nick) VALUES (123, "Benjamin", "Ben")
SELECT * FROM Authors;
INSERT INTO Authors (discord_id) VALUES (987) RETURNING author_id;
INSERT INTO Authors(discord_id) VALUES(987) RETURNING *;
