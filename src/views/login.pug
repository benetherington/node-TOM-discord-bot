extends layout.pug

append style
    include login.css

block title-bar
    //- No title bar here!

block content
    div#credentials
        form.card(id="login" action="/login" method="POST")
            input(name="username" placeholder="username" type="text")
            input(name="password" placeholder="********" type="password")
            button(type="submit") Log in
        div#message.card
                =errorMessage
